 void rearrange(long long *arr, int n) 
    {
     vector<long long int> v;
     for(int i=0;i<=n/2;i++)
     {
         v.push_back(arr[n-i-1]);
         v.push_back(arr[i]);
     }
     for(int i=0;i<n;i++)
     {
         arr[i]=v[i];
     }
    }
